public class MatrixChecker() {
	//takes in a single array and checks if it a palindroms
	public boolean isSymetrical(int [] aArray) {

		for(int x =0; x <aArray.length; x++) {
			if (aArray[x] == aArray[array.length] - x) {

			}
		}
	}
	//takes in a 2D array and checks if it's a palindrome
	public boolean isSymetrical(int [] [] aArray) {
		return false;
	} 
}